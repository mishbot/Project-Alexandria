>[!note] Merge Sort
>**Merge sort** is a [[2.3c.1 Divide-and-Conquer|divide-and-conquer]] sorting algorithm that splits an array into smaller subarrays, sorts each subarray recursively, and then merges the sorted subarrays to produce the final sorted array. The Python code of merge sort is given as

```python

```
