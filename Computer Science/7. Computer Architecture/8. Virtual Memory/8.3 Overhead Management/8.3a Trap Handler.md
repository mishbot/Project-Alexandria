>[!note] Trap Handler
>The **trap handler** is an OS process that manages system exceptions and interrupts (traps) generated by the CPU. Trap handlers work by completing the last instruction before the faulting instruction of a program, which it flushes it together with all subsequent instructions. The trap handler then enters CPU supervisor mode, and may sometimes be able to restore the program state, thereby resuming the execution of the program.
