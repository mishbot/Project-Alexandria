>[!note] Context Switch
>Context switches are OS switches between processes carried out by changing the internal state of the processor, thereby allowing a single processor to run many programs. Context switches are performed using OS hardware interrupts, and are handled by the [[8.3a Trap Handler|trap handler]], which saves the PC and page table base register (PTBR), and loads the next process' registers before returning to user mode.

